// import { createContext } from 'react';
import userStore from './user';
import shopStore from './shop';
import articleStore from './order';

export const stores = {
  userStore,
  shopStore,
  articleStore
};

// export const StoreContext = createContext(stores);